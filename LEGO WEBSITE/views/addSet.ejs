<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Set</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
  <header>
    <%- include('partials/navbar', {page: '/lego/addSet' }) %>
  </header>
  <main class="container mx-auto p-4">
    <div class="hero bg-base-200 rounded-lg p-6 ">
      <div class="hero-content text-center">
        <div class="max-w-md mx-auto">
          <h1 class="text-5xl font-bold">Add Set</h1>
          <p class="py-6">Use the below form to add a new Lego Set to the collection</p>
        </div>
      </div>
    </div>

    <form action="/lego/addSet" method="POST" class="mt-12 grid grid-cols-1 md:grid-cols-2 gap-4 ">
      <div>
        <label for="name" class="block text-sm font-medium text-gray-700">Name</label>
        <input type="text" id="name" name="name" required class="w-full border border-2 border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
      </div>
      <div>
        <label for="num_parts" class="block text-sm font-medium text-gray-700">Number of Parts</label>
        <input type="number" id="num_parts" name="num_parts" required class="w-full border border-2 border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
      </div>
      <div>
        <label for="year" class="block text-sm font-medium text-gray-700">Year</label>
        <input type="number" id="year" name="year" required class="w-full border border-2 border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
      </div>
      <div>
        <label for="img_url" class="block text-sm font-medium text-gray-700">Image URL</label>
        <input type="url" id="img_url" name="img_url" required class="w-full border border-2 border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
      </div>
      <div>
        <label for="theme_id" class="block text-sm font-medium text-gray-700">Theme</label>
        <select id="theme_id" name="theme_id" required class="w-full border border-2 border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
          <% themes.forEach(theme => { %>
            <option value="<%= theme.id %>"><%= theme.name %></option>
          <% }) %>
        </select>
      </div>
      <div>
        <label for="set_num" class="block text-sm font-medium text-gray-700">Set Num</label>
        <input type="text" id="set_num" name="set_num" required class="w-full border border-2 border-gray-300 rounded-md px-3 py-2 focus:border-blue-300">
      </div>
      <div class="md:col-span-2 text-center">
        <button type="submit" class="mt-4 px-4 py-2 btn btn-primary text-white font-bold rounded-md w-full">Add Set</button>
      </div>
    </form>
  </main>
</body>
</html>
