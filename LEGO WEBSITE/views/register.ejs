<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <link rel="stylesheet" href="/css/main.css" />
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/navbar', { page: 'register' }) %>
  <div class="container mx-auto p-4">
    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold">Register</h1>
          <p class="py-6">Please provide your credentials below</p>
          <% if (errorMessage) { %>
            <div class="alert alert-error">
              <span><%= errorMessage %></span>
            </div>
          <% } %>
          <% if (successMessage) { %>
            <div class="alert alert-success">
              <span><%= successMessage %></span>
            </div>
          <% } %>
          
        </div>
      </div>
    </div>
  </div>
  <div class="container mx-auto px-4">
    <form action="/register" method="POST">
      <div class="mb-6">
        <label class="font-bold">
          <span class="label-text">User Name</span>
        </label>
        <input type="text" name="userName" class="w-full p-2 border-2 border-gray-300 rounded-md" placeholder="User Name" value="<%= userName %>" required />
      </div>
      <div class="mb-6">
        <label class="font-bold">
          <span class="label-text">Password</span>
        </label>
        <input type="password" name="password" class="w-full p-2 border-2 border-gray-300 rounded-md" placeholder="Password" required />
      </div>
      <div class="mb-6">
        <label class="font-bold">
          <span class="label-text">Confirm Password</span>
        </label>
        <input type="password" name="confirmPassword" class="w-full border-2 p-2 border-gray-300 rounded-md" placeholder="Confirm Password" required />
      </div>
      <div class="mb-6">
        <label class="font-bold">
          <span class="label-text">Email Address</span>
        </label>
        <input type="email" name="email" class="w-full border-2 p-2 border-gray-300 rounded-md" placeholder="Email Address" required />
      </div>
      <div class="flex">
        <button class="bg-green-600 border border-green-500 text-white font-bold py-2 px-4 rounded-md hover:bg-green-700">Register</button>
      </div>
    </form>
  </div>
</body>
</html>
